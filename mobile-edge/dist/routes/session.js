export default async function route(_app) {
    // Legacy local mobile session token minting is intentionally disabled.
    // Mobile authentication now uses Accessing-owned `/access/*` session transport.
}
